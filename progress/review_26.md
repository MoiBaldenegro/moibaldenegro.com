# Review — feature 26

**Veredicto:** APPROVED

## Checkpoints
- C1 (REQ-26-01 — 00-agilismo next+related exactos): [x] — `src/content/architecture/00-agilismo.md:12-13` declara `next: /posts/01-diseño-detallado` (guion, slug real, no `01-diseño_detallado` de la 22) y `related: [/posts/02-principios-del-diseno-de-software]` (slug largo, no `02-principios`); test `REQ-26-01` en verde.
- C2 (REQ-26-02 — 01-diseño_detallado next+related exactos): [x] — `src/content/architecture/01-diseño_detallado.md:12-13` declara `next: /posts/02-principios-del-diseno-de-software` y `related: [/posts/03-principios solid]` (espacio canónico literal); test `REQ-26-02` en verde.
- C3 (REQ-26-03 — 02-principios next real + HIT conservado): [x] — `src/content/architecture/02-principios.md:12-13` declara `next: /posts/03-principios solid` y conserva `related: [/posts/00-agilismo]` (uno de los 2 HIT de la tabla del research, intacto); test `REQ-26-03` en verde.
- C4 (REQ-26-04 — 03-principios_solid related exacto + HIT conservado): [x] — `src/content/architecture/03-principios_solid.md:12` declara `related: [/posts/00-agilismo, /posts/01-diseño-detallado]` (conserva el HIT `/posts/00-agilismo`, corrige el MISS a guion); test `REQ-26-04` en verde.
- C5 (REQ-26-05 — toda ruta resuelve a un post.id de PostsRepository, sin filenames): [x] — `tests/next-related-hrefs-reales.test.mjs:173-196` inspecciona que el repositorio entrega `post.id` desde `entry.id` (`src/domain/repositories/posts-repository.ts:37`, verificado en disco) y resuelve cada href contra `PostsRepository.getPosts()` con loader inyectado (ids = slugs); `postIds()`/`entryIds()` derivan de campo `slug`, la única mención a `readdir` es un comentario que lo excluye (`next-related-hrefs-fix.test.mjs:17`); test `REQ-26-05` en verde.
- C6 (scope estricto frontmatter+tests, justificación REQ-43-06, estrechamiento REQ-22-07): [x] — la feature solo toca frontmatter `*.md` y tests (nuevo `tests/next-related-hrefs-reales.test.mjs` + ajustes `tests/next-post-data.test.mjs:11-19` y `tests/next-related-hrefs-fix.test.mjs:10-19`, ambos con justificación REQ-43-06 en encabezado; `content.config.ts`, entidad, repositorio, `[id].astro` y CSS sin cambios atribuibles a la 26 — los diffs de `[id].astro`/`post-next.css` en el árbol pertenecen al ciclo 23-25 sin commitear); REQ-22-07 se estrecha de "ningún espacio" a "sin espacios perimetrales" (`next-related-hrefs-fix.test.mjs:145-163`) porque el slug canónico `03-principios solid` contiene un espacio interior documentado como deuda fuera de alcance; `related-titles-design-align`/`related-card-model` intactos para la 27 como declara el informe.
- C7 (ciclo rojo→verde + ./init.sh): [x] — `progress/impl_26.md` documenta ROJO previo (`# pass 0 / # fail 5`, error `00-agilismo.md declara next "/posts/01-diseño_detallado" sin post.id entregado por PostsRepository (REQ-26-05)`) y VERDE posterior (subset 73/73); re-verificado por el revisor: subset 19/19 en los 3 tests de hrefs y `./init.sh` en verde (formato + tests 100% + build OK).
- C8 (arquitectura/convenciones + depends_on): [x] — sin `<style>` en `[id].astro`, sin lectura directa de JSON, sin dependencias nuevas, md fuera del conteo de 100 líneas de `src/`; `depends_on: []` en `feature_list.json:470` (nada que verificar); feature 26 en `in_progress` a la espera de este APPROVED, correcto según protocolo.

## Cambios requeridos (si aplica)
Ninguno.
