# Review — feature 23

**Veredicto:** APPROVED

## Checkpoints
- C1: [x] — Títulos resueltos en módulo `.ts` nuevo importado por la vista (REQ-23-01/02): `src/domain/related-titles.ts` (23 líneas) exporta `resolveRelatedTitles(posts, related): RelatedLink[]` (`{href, title}`; href desconocido degrada a su propio texto, línea 22); `src/pages/posts/[id].astro` (73/100) lo importa (línea 9), pasa `relatedLinks` por props desde `getStaticPaths` (línea 28) e itera `relatedLinks.map` con `href={item.href}>{item.title}` (líneas 64-66). Frontmatter sin lógica inline de colección en el marcado (el test REQ-23-02 lo aserciona: sin `getCollection`/`entry.data` en el marcado), `prerender` intacto (línea 37), cero `<script>`/JS de runtime. Repositorio sin extender y `post.css` intacto (test Convención: repo 98 líneas, post.css 100 líneas — suite verde lo confirma).
- C2: [x] — Botón con superficie/borde/texto claro (REQ-23-03/05): `.post__next-link` declara `background: var(--color-surface)` + `color: var(--color-text)` + `border: 1px solid var(--color-border)` (líneas 16-18 de `post-next.css`), hover solo `border-color: var(--color-accent)` (líneas 26-28); sin `background: var(--color-accent)` (asercionado por el test). Idioma de `.search-results__page-button` según `design.md`.
- C3: [x] — Lista con hairline + wash + subrayado (REQ-23-04/05): `.post__related-item` con `border-bottom: 1px solid var(--color-border)` (línea 51) + `:last-child: none` (líneas 55-57), hover con `background: var(--color-surface)` (líneas 59-61); `.post__related-link` en `var(--color-text)` 1.05rem con `text-decoration: underline` en hover (líneas 63-73); `.post__related-title` en `var(--color-text)` 1.35rem (líneas 34-39, escala reusada de `search-results.css`, D3 del design, sin tokens nuevos). Solo tokens: sin hex ni `rgb()/rgba()`, todo `var()` declarado en `tokens.css` (tests REQ-23-05/REQ-21-05 verdes). Los `padding … 12px` / `12px 6px` y el hairline `1px` replican el patrón canónico de `search-results.css` (`.search-results__item { padding: 16px 12px; border-bottom: 1px solid var(--color-border); }`, móvil `padding: 12px 6px`), no son valores sueltos fuera del sistema.
- C4: [x] — Responsive 768px + techo 100 líneas (REQ-23-06/07): `@media (max-width: 768px)` conserva botón y lista a `width: 100%` (líneas 75-91, test REQ-23-06 verde); `[id].astro` 73/100, `post-next.css` 91/100, `related-titles.ts` 23/100 (test REQ-23-07 verde).
- C5: [x] — Ajuste REQ-21-01/04 justificado + suite verde: `tests/related-posts-list.test.mjs` líneas 20-24 documenta el precedente REQ-43-06 (el test sigue a la presentación real; destinos `/posts/[id]` sin cambios, REQ-21-02/03/05/06/07 intactos). `./init.sh` ejecutado por el reviewer: formato ✔, tests al 100% ✔ (504/504), build ✔, «El entorno está perfecto». Nota: una primera corrida de `pnpm test` mostró 1 fallo transitorio (503/504) sin firma capturable; las dos corridas completas siguientes (504/504) y el `./init.sh` final quedaron en verde, mismo patrón transitorio ya documentado en `progress/impl_23.md`.

## Pregunta de revisión
¿Se escribió el test de cada archivo antes del código y en rojo, y la suite quedó en verde al final? Sí: `progress/impl_23.md` §"Ciclo rojo/verde" evidencia `tests/related-titles-design-align.test.mjs` en rojo antes del código (`ERR_MODULE_NOT_FOUND` del módulo inexistente + aserciones de superficie/hairlines incumplidas contra el estado previo) y en verde tras implementar (área tocada 24/24), con `./init.sh` final en verde re-verificado por el reviewer. ¿Se saltó una dependencia pendiente? No: la feature 23 declara `depends_on: [22]` en `feature_list.json` y la feature 22 está en `done`.

## Cambios requeridos (si aplica)
Ninguno.
