---
import "../styles/latest-articles.css";

import {  markdownPostRepository } from "../domain/repositories/posts-repository.ts";

const posts = await markdownPostRepository().getPosts();
console.log('LatestArticles: posts', posts);
---

<section class="latest-articles">
  <div class="latest-articles__list">
    {posts.map((post) => (
      <article  class="latest-articles__card">
        <a href={`/posts/${post.id}`} class="latest-articles__link">
          <img
            src={`/assets/content/${post.img}`}
            alt={post.title}
            class="latest-articles__image"
          />
          <h2 class="latest-articles__title">{post.title}</h2>
          <p class="latest-articles__description">{post.description}</p>
        </a>
      </article>
    ))}
  </div>
</section>
